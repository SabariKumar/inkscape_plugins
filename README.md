To install:
1. Open Inkscape Preferences -> System -> User Extensions
2. Copy both the .py and .inx files for each extension to the User Extensions folder
3. Restart Inkscape - extensions will be in Extensions -> Generate
